{
  "name": "Beam Python flex template",
  "description": "Beam example for python flex template.",
  "parameters": [
    {
      "name": "testing the google trigger",
      "label": "testing google triggers.",
      "helpText": "Name of the input PubSub subscription to consume from.",
      "regexes": [
        "projects/[^/]+/subscriptions/[a-zA-Z][-_.~+%a-zA-Z0-9]{2,}"
      ]
    }
  ]
}
